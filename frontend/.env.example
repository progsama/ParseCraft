# Copy to .env.local for local dev. Do not commit .env.local.

# Backend base URL (no trailing slash). Use http://localhost:8080 for local/Docker backend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
